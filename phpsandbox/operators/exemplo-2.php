<?php

$a = 1;
$b = 2;

$a > $b; // Greater than
$a < $b; // Less than
$a >= $b; // Greater or equal than
$a <= $b; // Less or equal than
$a == $b; // Equals (value only)
$a === $b; // Equals (value and type)
$a != $b; // Different (value only)
$a !== $b; // Different (value and type)

?>